%% Function to generate the M matrix in the matrix equation

%function definition
function M = GenerateM(N, alpha, beta)
    % generate 0 matrix
    
    % fill middle rows
    
    %fill upper row
    
    % fill lower row
    
    M = 0;